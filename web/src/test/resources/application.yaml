spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

  liquibase:
    label-filter: "!seed"

  cloud:
    config:
      enabled: false

application:
  postgres-image: postgres:17-alpine
